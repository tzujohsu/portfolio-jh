alias python3=python3.10
source .venv/bin/activate
git rm -rf --cached .


python -c "for d in __import__('importlib.metadata').metadata.distributions(): print('{:>12.3f} KiB   {}'.format(sum(0 if not f.locate().is_file() else f.locate().stat().st_size for f in d.files) / 1024, d.name))"


pysqlite3-binary
beautifulsoup4
st-pages
openai
accelerate
chromadb==0.5.15
langchain_chroma
langchain_community
langchain-openai
langchain_text_splitters
streamlit_timeline
sentence-transformers
langchain_huggingface

[[pages]]
path = "pages/timeline.py"
name = "ChronoEvents: Timeline Generator"
icon = ":hourglass_flowing_sand:"